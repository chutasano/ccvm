#pragma once

enum b_ops
{
    B_PLUS, // + operator
    B_EQ,   // == operator on num
    B_LT,   // <
    B_GT,   // >
    B_LE,   // <=
    B_GE   // >=
};

enum u_ops
{
    U_NEG, // - (negation) operator
    U_NOT
};

